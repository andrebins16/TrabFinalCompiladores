int a; 
int b; 
int c;
int ma;
int me;

void main() {  
    a = b = c = 10;

    ++b;
    c--;

    me = a<=b && a<=c ? a : b <= c ? b : c; 
    ma = a>=b && a>=c ? a : b >= c ? b : c;   

    write (" a (10)  =  ", a );
    write (" b (11)  =  ", b );
    write (" c (9)   =  ", c );
    write (" me (9)  =  ", me );
    write (" ma (11) =  ", ma );
}